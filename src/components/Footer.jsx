import React from 'react';

const Footer = () => {
    return (
        <div>
            <footer className="footer">
                <span className='"text-muted'>Company ABC</span>
            </footer>
        </div>
    );
};

export default Footer;